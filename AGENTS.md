# Contribution workflow

All changes to this repository must go through a pull request. Never commit or
push changes directly to the default branch.

For every change:

1. Create a focused branch from the latest default branch.
2. Make and validate the change on that branch.
3. Push the branch and open a pull request describing the change and its checks.
4. Merge only through the pull request after the required review and checks.
